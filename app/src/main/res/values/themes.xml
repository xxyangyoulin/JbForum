<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.JbForum.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@android:color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="postSplashScreenTheme">@style/Theme.JbForum</item>
    </style>

    <style name="Theme.JbForum" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar" tools:targetApi="23">true</item>
    </style>

    <style name="Theme.JbForum.ImagePreview" parent="Theme.JbForum">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:colorBackgroundCacheHint">@null</item>
        <item name="android:windowAnimationStyle">@style/WindowAnimationNone</item>
    </style>

    <style name="Theme.JbForum.ShareImport" parent="Theme.JbForum.ImagePreview">
        <item name="android:windowAnimationStyle">@style/WindowAnimationNone</item>
    </style>

    <style name="WindowAnimationNone">
        <item name="android:activityOpenEnterAnimation">@anim/none</item>
        <item name="android:activityOpenExitAnimation">@anim/none</item>
        <item name="android:activityCloseEnterAnimation">@anim/none</item>
        <item name="android:activityCloseExitAnimation">@anim/none</item>
    </style>
</resources>
